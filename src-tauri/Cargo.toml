[package]
name = "supernotes"
version = "0.1.0"
description = "Minimalist note-taking and productivity app"
authors = ["Supernotes"]
edition = "2021"

[lib]
name = "supernotes_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled"] }
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "2"
ureq = { version = "2.12", features = ["json", "tls"] }
# keyring 3 has no default features. Without a platform store, macOS/Windows/Linux
# silently use an in-memory mock that does not persist across Entry::new — set
# returns Ok and the next get is NoEntry, so Test omits Authorization.
keyring = { version = "3", features = ["apple-native", "windows-native", "linux-native"] }

[target.'cfg(any(target_os = "macos", target_os = "windows"))'.dependencies]
cpal = "0.15"

[target.'cfg(target_os = "macos")'.dependencies]
whisper-rs = "0.16"
objc2 = "0.6"
objc2-foundation = "0.3"
